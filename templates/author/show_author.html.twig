<!DOCTYPE html>
<html>
<head>
    <title>Détails de l'Auteur</title>
</head>
<body>
    <h1>{{ author.name }}</h1>
   <h1> <img src="{{ asset(author.picture) }}" alt="{{ author.name }}"></h1>
    <p>Email: {{ author.email }}</p>
    <p>Nombre de livres: {{ author.nb_books }}</p>
    <a href="{{ path('app_authors') }}">Retour à la liste des auteurs</a>
    

</body>
</html>
